rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for sandboxed prototype to prevent any permission issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
